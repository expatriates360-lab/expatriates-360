[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Increase timeout for all serverless functions (max 26s on free plan)
  node_bundler = "nft"

[[plugins]]
  package = "@netlify/plugin-nextjs"
